import 'package:flutter/material.dart';

class LocationReasonDialog extends StatelessWidget {
  const LocationReasonDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dialog();
  }
}
